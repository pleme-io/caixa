# CRDs live here — they're regenerated from caixa-crd's Rust types by:
#
#     cargo run -p caixa-crd --bin dump-crds > templates/crds/caixa.pleme.io_crds.yaml
#
# Keep the file committed; CI fails if it drifts from the Rust source.
# A future Helm helper pre-hook can regen automatically.
